# Admin Report Desk Changelog

## 1.0.29

- **Fix (критично):** после autoupdate bootstrap больше не делает reload — сразу грузит ядро в той же сессии. Раньше reload убивал скрипт и F7 не работал.

## 1.0.28

- **Fix (критично):** bootstrap ждёт 2 сек перед reload после обновления — скрипт больше не «умирает» при подключении к серверу.
- **Fix:** после reload bootstrap сразу грузит core без повторной полной синхронизации.
- **Fix:** autoupdate корректно сверяет `.pending`-файлы при проверке SHA256.

## 1.0.27

- **Checker:** clist-цвета фракций, HUD руководства/админов/друзей, настройки лидеров по группам, sync `/leaders`.
- **UI:** строка поиска на вкладке ТС как на Скинах (без метки «Поиск», кнопка «Очистить» не вылезает).
- **Core:** модули fs/sha256/zip/wm_dispatch/cmd_binds/spectate_fsm, атомарное сохранение config, cleanup при reload.
- **Repo:** runtime-зависимости (mimgui, samp, encoding, iconv) в git — клон репозитория работает без ручной докачки.

## 1.0.14

- **Autoupdate v2 (критично):** manifest v2 с SHA256 каждого файла — обновление по хешам, не по версии launcher.
- **Атомарная установка:** staging → verify SHA256 → commit; 3 retry на каждый download.
- **Единый state:** `report_desk/_install_state.json` (+ миграция с `_core_version.txt`).
- **Self-heal:** при core error или `/deskrepair` — полная переустановка всех компонентов из manifest.
- **Диагностика:** `/deskupdate` — что установлено vs remote; `/deskrepair` — принудительный repair.
- **Publish:** `publish_release.ps1 -Publish -Push` — автоматический `gh release create/upload` всех 7 артефактов.
- **Pending:** launcher и autoupdate обновляются через `.pending` (Windows file lock).

## 1.0.13

- **Fix (критично):** `report_desk_deps.lua` / `report_desk_autoupdate.lua` перенесены в `lib/` — MoonLoader больше не грузит их как отдельные скрипты из корня.
- **Fix:** синтаксис PowerShell-строк (`[[...]]`) — устранена ошибка `'}' expected near 'mimgui'`.
- **Fix:** launcher отключает старые копии в корне (`.off`) и грузит модули из `lib/`.

## 1.0.12

- **Fix (критично):** убран блокирующий `wait(isSampAvailable)` в launcher — после `/reload` при подключении к серверу скрипт больше не зависает и доходит до ядра.
- **Fix:** `iconv.dll` качается до загрузки core; bootstrap не валит скрипт при временном сбое lib/iconv.
- **Fix:** shim `sampfuncs` для bundled `samp.raknet`; launcher обновляется через `.pending` (Windows file lock).
- **Log:** все сообщения autoupdate дублируются в `moonloader.log`.

## 1.0.11

- **Autoupdate bootstrap:** `/reload` сам качает `lib/`, `iconv.dll`, launcher, deps, autoupdate и core — без переустановки zip.
- **Core bundle:** `samp.events`, `vkeys`, `encoding`, `vector3d` вшиты в ядро — работает даже на старом launcher 1.0.9.
- **iconv:** автоскачивание `iconv.dll` перед загрузкой encoding (autoupdate + bootstrap fallback).
- **Runtime pack:** малый `report_desk_runtime_libs.zip` (~100 KB) на GitHub Release для bootstrap.

## 1.0.10

- **Fix (критично):** zip теперь включает `lib/samp`, `encoding`, `iconv`, `vkeys`, `vector3d` — чистая установка больше не падает на bootstrap.
- **Fix:** launcher ждёт SAMP/SAMPFUNCS вместо мгновенного `return` (скрипт не «молчаливо» умирал при раннем старте).
- **Fix:** в core вшит `report_desk_user_defaults` (миграция сценариев без файла на диске).
- **Deps:** явная проверка runtime-библиотек + сообщение в чат/log при неполной установке.

## 1.0.9

- **Сценарии v2:** 81 переработанный сценарий в `admin_report_desk_user.default.lua`; авто-merge новых сценариев у существующих юзеров (`scenarios_pack_version`, без перезаписи своих).
- **SP /ans (C):** state machine KEYDOWN/KEYUP — нет фантомной отправки, стабильный фокус при повторном открытии.
- **SP input:** ans bar не дергает SP HUD; корректный захват клавиатуры без блокировки WM_CHAR.
- **HUD:** persistence позиций checker/cheats/stats после `/reload`; drag без прыжков; checker read-only.
- **Bundle:** добавлен `report_desk_sp_refresh`; в core вшит pack сценариев для миграции.

## 1.0.8

- **Release pipeline:** verify SHA256 (dist = git = zip), `build_manifest.json`, снятие `assume-unchanged`, правильный git commit, `zip_url` в manifest.
- **Release sync:** bundle полностью совпадает с dev (`scenario_learn`, `remote_chat`, все SP-модули).
- **SP:** handshake `/sp` — `expectSpectateOff`, `isAwaitingSpectate`, защита от ложного `toggle(false)`, deferred `/st`.
- **Autoupdate:** `core_url` → GitHub Release asset (+ raw fallback в manifest).
- **Bundle verify:** проверка всех входных файлов и ключевых символов при сборке.

## 1.0.7

- Perf: кэш ellipsize, меньше лишней работы UI при закрытом окне; оптимизация startup-сообщения.
- SP: восстановлены зум колёсиком (`spectate_camera`), HUD клавиш `/sp` (`sp_keys_hud`), синхронизация session/stats без лишних reset.
- SP: чат `/ans` — **C** открыть/закрыть (Enter отправить, Esc закрыть).
- Чекер: ГА/ЗГА (Arthas_Bartolomeo, Amattore_Adderio) — отдельные теги и зелёный цвет, не сбрасываются после `/adms`.
- Маркер/clickwarp: посадка в ТС по SAMP id (как AdminTools getcar); команда **`/acar ID`** для проверки.
- Bundle: в релизное ядро добавлены `spectate_camera` и `sp_keys_hud` (zip без lib/ больше не падает на /sp).

## 1.0.6

- Fix: ГА/ЗГА (Arthas_Bartolomeo, Amattore_Adderio) — отдельные теги и зелёный цвет в блоке «Админы», не сбрасываются после `/adms`.
- SP: чат `/ans` с целью — **C** вместо Alt (Enter — отправить, Esc — закрыть, C — открыть/закрыть).

## 1.0.5

- Fix: core error после 1.0.4 — app-модули снова в одном chunk (общий scope), checker отдельно (лимит 200 locals).

## 1.0.4

- Fix: «локальных переменных больше 200» при загрузке zip-ядра — каждый модуль bundle в отдельном chunk с общим env.

## 1.0.3

- Fix: у zip-пользователей не скрывалось серверное GTA-меню /sp — хуки блокировки перенесены в bundle (sampev + RPC).

## 1.0.2

- Fix: бесконечный reload autoupdate (manifest новее launcher beta.*) — `_core_version.txt`, загрузка ядра `.lua` вместо старого `.luac`.

## 1.0.1

- Fix: autoupdate для `1.0.0-beta.*` — корректное сравнение версий; manifest `1.0.1` подтягивает ядро у клиентов beta.1/beta.2.

## 1.0.0-beta.3

- Кастомное меню `/sp` (ImGui): навигация ↑↓, подтверждение Пробел, звук меню (опция в настройках).
- Spectate: HUD статистики, ручная «Статистика» (серверный диалог), «Достать из воды», без флуда стрелками.
- Чекер: подписи «Админы:» / «PR и технари:», читаемый HUD; стрелки не блокируются в чате/диалогах.
- Выход из spectate при ban/kick цели; grace при Alt+Tab.
- Заголовок меню `/sp`: контрастный ник и ID.

## 1.0.0-beta.2

- Автоустановка mimgui (`report_desk_deps.lua`), mimgui в zip.
- Одно сообщение в чат: загрузка + статус обновлений (CP1251).
- Исправлен двойной запуск (stub только в `tools/`, не в moonloader).

## 1.0.0-beta.1

- Установочный zip: общие сценарии/автоответы (`config/`) и превью скинов/ТС (`res/`).

## 1.0.0-beta

Первый публичный beta-релиз для распространения через GitHub.

- Репорты, автоответы, сценарии, спектейт + HUD статистики
- Чекер (админы/лидеры), каталог скинов и ТС
- Автообновление ядра с GitHub Releases
- Фиксы: dialog flood, spectate `/st`, чекер HUD, превью скинов/ТС

## 3.49.12

- Скины/ТС: превью в сетке снова грузятся при скролле (сайдбар больше не сбрасывает очередь загрузки каждый кадр).
- Закрытие окна / выгрузка скрипта: `deskTexPipeline.shutdown` + безопасная остановка IO-воркера.
- Чат: кнопки сценариев снова появляются у сообщений с совпавшими ключевыми словами (убрана ошибочная привязка только к последнему сообщению игрока).
- Чекер: скрытые лидеры сохраняются после перезагрузки и `/leaders` sync; HUD лидеров — компактный список (ник + должность, без заголовков организаций).
- Закрытие окна (крестик): отложенный `ReleaseTexture` вне кадра ImGui + `deskTexPipeline.halt/shutdown` (фикс краша).
- Чекер: ручные `/leaders` и `/adms` снова показывают серверный диалог; перехват только при авто-sync чекера.
- Spectate/stats: не закрывает посторонние окна (`/mn` и др.); убран спам `SendDialogResponse` (dialog flood).
- Spectate/stats: детект диалога `/st` без `string.lower` по кириллице; перехват после своего `st` по полям; закрытие одним ответом + `sampCloseCurrentDialogWithButton`.

## 3.49.11

- Perf: продакшн-пайплайн превью (`report_desk_tex_pipeline`) — disk IO в `lua_thread`, GPU upload с бюджетом 6/кадр на main thread
- Perf: загрузка только видимых ячеек + выбранный ID; очередь IO обрезается при скролле; LRU VRAM 96/64
- Perf: staging без дублирования bytes+VRAM; убран прогрев всего каталога и стартовый оверлей
- Assets: только PNG/JPG — убран выбор DDS/PNG и двойные namespace превью; один файл на ID для сетки и сайдбара
- Perf: async IO + LRU 72/48 без лишней логики форматов

## 3.49.10

- Fix: ложные уведомления о входе после reload — во время прогрева игроки из rebuild помечаются «уже виденными»; join не шлётся если ID уже в онлайне
- Fix: прогрев join-уведомлений срабатывает по таймеру в `checkerTick`, не только при rebuild
- Fix: одно уведомление на вход (админ > лидер > друг), один звук; отключение только для отслеживаемых игроков

## 3.49.9

- Fix: админы в HUD чекера отображались дважды — при reload старая подписка `imgui.OnFrame` не снималась; отписка через `_G.__desk_checkerHudFrame` + `deskUninstall`
- Fix: дедупликация онлайн-списков по `playerId` при rebuild и публикации HUD

## 3.49.8

- Fix: HUD чекера пустой — `checkerAdminColor` вызывал `checkerIsSpecialLevel` до объявления (в логе: `checkerIsSpecialLevel (a nil value)`), отрисовка падала каждый кадр

## 3.49.7

- Fix: HUD чекера пустой после reload — `checkerState`/`checkerOnline` больше не подхватываются из старого `_G`; снимок HUD копируется, кадр переустанавливается
- Fix: отрисовка HUD через `__desk_drawCheckerHud` + авто-rebuild если в каталоге есть админы, а в HUD 0

## 3.49.6

- Fix: скрипт не загружался (`main function has more than 200 local variables`) — чекер снова отдельным chunk после core

## 3.49.5

- Fix: HUD чекера пустой при живом онлайне — снимок в `deskCache.checkerHud`, свой `OnFrame` (`installCheckerHudFrame`)
- Fix: ложные «подключился лидер» при входе/перезагрузке — прогрев 10 с + уже видимые ID не уведомляются
- Fix: уведомления лидеров отдельно (`checker_notify_leader_join`, по умолчанию выкл.); админы — галочка «Подключение (админы)»

## 3.49.4

- Fix: чекер «мёртвый» в игре — онлайн/HUD через общий nick-кэш Report Desk + `gamestate` connected (не только `sampIsLocalPlayerSpawned`)
- Fix: авто-синх снова шлёт `/admins` в чат (надёжно); `/leaders` — диалог всегда закрывается после нашего запроса
- Fix: при ESC/AFK чекер продолжает обновлять HUD; авто-синх только ставится на паузу
- Fix: ошибки диалога чекера пишутся в `moonloader.log`

## 3.49.3

- Fix: чекер снова работает — восстановлены перехват диалогов `/adms` и `/leaders`, авто-синхронизация (4 и 15 мин), хук `checkerOnShowDialog`
- Fix: ручная синхронизация и авто-синх шлют `/adms` + `/leaders` (каталог обновляется из диалога, окно закрывается)
- Fix: захват `/admins` из чата не обрывается на посторонних строках; `checker_auto_sync` всегда включён
- Чекер: после паузы (ESC/AFK) синхронизация откладывается на 6–10 с; блок при открытом /reps или диалоге

## 3.49.2

- Fix: чекер HUD пустой — возвращено прямое сканирование `sampIsPlayerConnected` + `sampGetPlayerNickname` (0..maxId), как в рабочей версии
- Чекер: `checkerBuildNickIndex()` — свой индекс ников, не зависит от `findPlayerIdByNick` из core-чанка

## 3.49.1

- Fix: чекер HUD пустой (0 админов) — `checkerPlayerConnectedSafe` вызывался до объявления (Lua 5.1 forward-ref)
- Fix: rebuild онлайн-списка принудительно обновляет nick-кэш; `lastRebuild` ставится до попытки, без спама в лог

## 3.49.0

- Чекер: продакшн-модель — онлайн = каталог × nick-кэш Report Desk (без авто /admins /adms /leaders)
- Чекер: `checkerRebuildOnline()` через `findPlayerIdByNick`, join/quit/streamIn, страховочный rebuild каждые 15 с
- Чекер: синхронизация каталога только по кнопке или ручному `/admins` (пассивный захват чата)
- Удалены: авто-синх при входе, периодический /admins 4 мин, /leaders, scan 0..1000, onlineIndex, merge-хаки

## 3.48.6

- Fix: парсинг `/admins` — захват не обрывался на посторонних строках чата (каталог не сжимался до 1 админа)
- Fix: ответ «Админы онлайн» сам запускает захват, даже если хук команды не сработал; `/admins` в чате тоже ловится
- Fix: из строки берётся `[id]` — HUD сразу показывает всех из ответа сервера (формат `Nick[id] (S3 lvl) Voice`)
- Автосинх `/admins` в merge-режиме, в лог: `checker: captured N admins`

## 3.48.5

- Fix: чекер не видел игроков — `sampGetMaxPlayerId(false)` + fallback `MAX_PLAYER_ID` (1000), как в остальном Report Desk
- Fix: `/admins` не уходил при открытом диалоге/alt-tab — блокировка только ESC-меню; диалог мешает только `/leaders`
- Fix: начальный синхр повторяется, пока `/admins` реально не отправлен (не помечается done при неудаче)
- Чекер: синхр после спавна работает даже при паузе; в лог пишется размер каталога; `/repsdebug` — rescan и sync

## 3.48.4

- Fix: чекер — rescan больше не блокируется «паузой»/ESC (tick не обрывался на isGamePaused)
- Fix: `/admins` не блокируется открытым /reps; начальный синхр через 3 с после спавна
- Fix: парсинг админов без `[id]` в строке; хук `onPlayerStreamIn` для уже онлайн игроков
- Fix: при спавне сразу rescan (lastRescan=0), без lua_thread

## 3.48.3

- Fix: чекер снова показывает админов — убрана 8 с блокировка `checkerIsReady`, возвращён rescan каждые 5 с и первый rescan через 2 с
- Fix: `/admins` и `/leaders` снова отправляются после спавна (~5 с), без лишних условий
- Чекер: join/quit по-прежнему без SAMP в хуке (антикраш); HUD и список онлайн как в 3.47.1

## 3.48.2

- Fix: чекер снова синхронизируется при входе — `/admins` ~10 с после спавна, затем `/leaders` если есть лидеры в каталоге
- Чекер: начальный синхр всегда при спавне; повтор каждые 4 мин если включён «Автосинх при входе»
- Чекер: вкладка «Синхронизация» — тоггл автосинха и кнопка «Синхронизировать сейчас»
- Fix: прогрев автосинха 10 с вместо 60 с; `/admins` в чат (безопасно), не `/adms` диалог

## 3.48.1

- Fix: краш чекера — SAMP-вызовы (`sampGetPlayerNickname`, полный rescan) только после 8 с grace в main loop, не в join/chat хуках
- Fix: join/quit использует ник из события без SAMP API; rescan и AFK poll отложены до `checkerIsReady()`
- Fix: rescan после alt-tab и смены каталога через очередь, не синхронно при resume

## 3.48.0

- Ревизия чекера: event-driven онлайн-список (join/quit), убран тяжёлый rescan каждые 5 с
- Чекер: инкрементальный onlineIndex + O(1) catalogIndex; AFK-поллинг раз в 15 с только по отслеживаемым ID
- Fix: позиция HUD чекера сохраняется при ручном перетаскивании — без clamp при загрузке, `checker_hud_h` для off-screen guard
- Чекер: удалён мёртвый dialog/leaders sync код (краш SA-MP); автосинх только `/admins` в чат
- Fix: единые дефолты `checker_auto_sync=false`, `checker_hud_y=8`, `checker_show_leaders=false`

## 3.47.2

- Fix: сценарии больше не уходят через автоответ — только по кнопке в чате с игроком (time/GG в автоответах без изменений)

## 3.47.1

- Fix: чекер снова показывает админов/лидеров — убраны лишние блокировки (grace/gamestate), rescan каждые 5 с после спавна
- Fix: join/quit обновляет список сразу после спавна; первый rescan через 1.5 с
- Чекер: классическая схема — каталог + сопоставление ников онлайн (как обычные ML-чекеры)

## 3.47.0

- Fix: safe mode чекера — нет хука диалогов, нет /leaders автосинха, нет rescan каждые 5 с
- Fix: SAMP-вызовы только через 10 с после спавна (checkerIsReady); join/stream-in до этого игнорируется
- Fix: автосинх выключен по умолчанию; только /admins в чат если включить в настройках
- Fix: убран rescan из auto-promote и периодического tick

## 3.46.0

- Fix: краш SA-MP 0x72765E00 — чекер больше не поглощает RPC диалога (return false всегда); это ломало клиент
- Fix: автосинх админов через /admins в чат (без окна /adms); только /leaders использует диалог
- Fix: закрытие /leaders только когда sampIsDialogActive() подтверждает открытое окно

## 3.45.1

- Fix: краш при автосинхе — убран sampSendDialogResponse (диалог гасится через consume RPC, без «ответа» на несуществующее окно)
- Fix: rescan/SAMP-вызовы вынесены из onShowDialog в отложенный поток (нельзя трогать SAMP внутри RPC-хука)
- Fix: безопасные sampGetPlayerNickname / sampGetPlayerColor в rescan и HUD

## 3.45.0

- Fix: краш при входе — чекер полностью неактивен до gamestate=connected + спавн (никаких SAMP-циклов на экране логина)
- Fix: диалоги /adms и /leaders перехватываются только когда скрипт сам их запросил; логин и ручные команды не трогаются
- Fix: автосинх и кнопка «Синхронизировать» сами закрывают окна (как /st в спеке), без ручного закрытия
- Чекер: /adms и /leaders из чата вручную — окно остаётся на экране как обычно

## 3.44.9

- Fix: краш при входе на сервер — чекер больше не вызывает sampSendDialogResponse и не перехватывает диалоги логина
- Fix: rescan/SAMP-цикл 0–999 только после спавна в мире (раньше срабатывал уже на экране подключения)
- Fix: HUD и автосинх /adms + /leaders включаются только в игре; прогрев автосинха ~45 с после спавна
- Чекер: окна /adms и /leaders при автосинхе остаются на экране — закройте сами; данные всё равно подтягиваются

## 3.44.8

- Fix: краш чекера — закрытие /adms и /leaders перенесено из хука диалога в отложенный поток (150 ms)
- Fix: синхронизация каталога тоже отложена, без тяжёлой работы внутри onShowDialog
- Fix: автосинх только когда игрок заспавнен, без паузы/alt-tab и без активного диалога
- Fix: упрощён HUD чекера (без draw list по GetItemRectMin)

## 3.44.7

- Чекер: список в HUD снова показывает админов (убран ложный фильтр sampIsPlayerConnected в отрисовке)
- Чекер: /leaders и /adms вручную снова открывают окно; автосинхрон скрывает только свои вызовы
- Чекер: автосинх /adms + /leaders через ~3–8 с после загрузки (было 45+ с)
- Чекер: безопасные SAMP-вызовы после alt-tab (pcall, сброс drag/sync при паузе)
- Fix: HUD чекера — заголовок через draw list (убраны артефакты с цифрами)

## 3.44.6

- Чекер: распознавание лидеров/друзей без учёта регистра; rescan сразу после загрузки каталога
- Чекер: перетаскивание HUD за заголовок «Чекер» (как у читов)
- Чекер: лидеры в HUD и настройках — блоками (Правительство, LS/LV/SF, силовые, СМИ и т.д.), без внутреннего скролла
- Убран переключатель «Запоминать позицию HUD» — позиция всегда сохраняется
- Автоответы: включены сценарии из вкладки «Сценарии» (enabled + reply), не только time/GG

## 3.44.5

- Fix: ползунки-тогглы в читах (Godmode, WH, AB), автоответах и сценариях — у каждого переключателя свой ImGui ID (раньше «Включен»/«Выключен» дублировались и клики не работали)

## 3.44.4

- Fix: HUD чекера снова показывается (убрана блокирующая проверка спавна в ImGui)
- Чекер: друзья — только добавление по нику и удаление из списка
- Чекер: лидеры в настройках сгруппированы по организации (ник + org · role)

## 3.44.3

- Fix: скрипт не стартовал — `sampIsLocalPlayerSpawned` в ImGui Condition до готовности SAMP (opcode 0B61)
- Fix: при краше до загрузки конфига больше не перезаписывается `admin_report_desk.lua` дефолтами

## 3.44.2

- Fix: краш при входе — перехват только диалогов с заголовком admins/leaders (без окна по таймеру)
- Fix: закрытие sync-диалога одним `sampSendDialogResponse` (убран `sampCloseCurrentDialogWithButton`)
- Fix: HUD/rescan/auto-sync чекера только после спавна + пауза 30–45 с

## 3.44.1

- Fix: чекер больше не закрывает посторонние диалоги после /adms и /leaders (краш при входе)
- Fix: безопасная отрисовка HUD (цвет игрока, отключившийся ID)
- Fix: позиция HUD чекера сохраняется после перетаскивания (clamp + flush)
- Чекер: вкладка «Друзья» — добавление по нику, списком и из онлайна
- Чекер: галочки для скрытия отдельных лидеров в HUD (настройки сохраняются)

## 3.44.0

- Чекер: перехват и закрытие диалогов /adms и /leaders (в т.ч. при авто-синхронизации)
- Чекер: цветной ник админа в уведомлениях подключения/отключения по уровню

## 3.43.9

- Чекер: S1/S2/S3 — отдельная категория (не путается с lvl 1/2/3), свои цвета и подпись

## 3.43.8

- Чекер: упрощены настройки — убраны кнопки синхр./импорт/сброс, поясняющий текст, «показывать с /reps»
- HUD чекера скрывается при открытом /reps; синхронизация списков всегда в фоне

## 3.43.7

- Fix: «update failed: no manifest» — stub не видел `require('report_desk_app')`, висел 25с на GitHub; stub отключён в dev (.off)
- Fix: autoupdate сразу пропускает placeholder URL без timeout
- Чекер и прогрев каталога: пауза при ESC/AFK; синхронизация откладывается на 6–10с после выхода из паузы

## 3.43.6

- Чекер: оптимизация онлайна (один проход по игрокам, без лишних rescan на join/quit)
- Чекер: авто-синхронизация не запускается при открытом диалоге или /reps; /adms и /leaders раздельно (4 и 15 мин)
- Чекер: убран блок «Каталог» из настроек — списки обновляются с сервера

## 3.43.5

- Чекер: диалог /leaders не показывается после синхронизации (как stats)
- Чекер HUD: плоский список админов по уровню (цвет + lvl), без групп «Уровень N»
- Убрана настройка «Группировать по уровню»; убрана метка `[#]`
- Fix: кодировка текста во вкладке «Синхронизация»

## 3.43.4

- Fix: `imgui.ImBool` заменён на `new.bool` (mimgui API)
- Fix: явный `main()` в `admin_report_desk.lua`
- Fix: все модули перенесены в `lib/` — MoonLoader больше не грузит их дважды

## 3.43.3

- Fix: чекер в отдельном блоке — экспорт imgui/settings в shared env
- Fix: модули report_desk_* не выполняются при автозагрузке MoonLoader

## 3.43.2

- Fix: скрипт не стартовал (повреждённые модули chat/config/cheats/threads/profanity)
- Fix: лимит Lua 200 locals — чекер грузится отдельным блоком
- Fix: MoonLoader больше не запускает `report_desk_main.lua` и `report_desk_checker.lua` сами по себе

## 3.43.1

- Чекер: синхронизация каталога с `/admins` (merge), `/adms` (replace), `/leaders` (диалог, должность)
- Чекер: авто `/adms` + `/leaders` каждые 4 мин после спавна; звук при подключении
- Чекер: авто-назначение админов из srv (-65281); команда `/checker` убрана

## 3.43.0

- Чекер: HUD на ImGui (лидеры, друзья, админы с группировкой по уровню и цветами)
- Чекер: автообновление при join/quit + периодический rescan; уведомления в чат
- Чекер: авто-добавление админов из srv-сообщений (-65281); `/checker admin/leader/friend add/remove`
- Чекер: каталог в `config/admin_report_desk_user.lua`; импорт из `AdminTools/checker/*.txt`
- Вкладка «Чекер» в /reps: настройки HUD, каталог, импорт

## 3.42.8

- Команда `/hist [id]` — отправляет `/history ник` для онлайн-игрока

## 3.42.7

- Автоответы: пауза 120 ms перед отправкой (SAMP успевает выдать ans ID); retry без ложного dedup
- Автоответы: dedup по normalizeMatchText (спасибо / спасибо! — одно и то же); cooldown не сбрасывается на каждый репорт
- GG: триггер `gg`; перепроверка auto_time/auto_gg toggles в processAutoRules
- Сценарии «Анонсы /news»: keywords посмотреть+список+собес, как+посмотреть+собес, список+собеседован

## 3.42.6

- Сценарии: stem-матчинг (собеседований → собеседование); кнопки у последнего вопроса + в composer
- Keywords «Анонсы /news»: список+собес, посмотреть+собес, /news и др.

## 3.42.5

- Автоответы: убран блок «Проверка»; подсказка только `{datetime}`

## 3.42.4

- AirBreak: шаг Q/E при удержании 0.04 (было 0.02)

## 3.42.3

- Вкладка «Автоответы»: layout как в «Настройках» (скролл, секции, flat inputs); убраны сломанные nested child с height=0

## 3.42.2

- Автоответы «время» и «GG»: keywords в коде (exact match), без редактора списка в UI
- Вкладка «Автоответы»: только вкл/выкл и текст; отдельно time / GG + общий master-switch
- GG: расширены варианты (спасибо, благодарю, спс/sps, merci, thx, раскладка cgfcb/kfujlfh); убран опасный триггер «j»
- Время: ешьu/eshyu, /c 60, «который час», «сколько времени» и др.
- Настройки: `auto_time_enabled`, `auto_gg_enabled`, `time_reply`; миграция из старых `rules` в user.lua

## 3.42.1

- Сценарии: один вопрос — один ответ; разбиты смешанные (телефон, чаты, лицензия, БП, кредиты, бензин/механик и др.)

## 3.42.0

- ТС: только «Количество» для спавна; цвета убраны из UI (`/carsp` с c1=c2=0)
- Настройки: уровень админа 1–4 (clamp при загрузке конфига)
- UI биндов: единый `drawDeskBindRow` (KeyCap + меню + accent chips) в Настройках и Читах
- Сценарии: расширены keywords по chatlog (машина, телефон, лицензия, гос, работа, бензин)

## 3.41.6

- Composer: после отправки ответа (Enter/кнопка) фокус остаётся в поле ввода — можно сразу писать следующее сообщение

## 3.41.5

- Камера: единое правило `deskImguiNeedsInput()` — ImGui не перехватывает мышь без нужды (в т.ч. в `/sp`)
- Убран per-frame `deskReleaseImguiCapture`; сброс курсора только на переходах (F7, `/sp`, unload)
- `deskSyncSpectateState(false)` и marker off восстанавливают камеру; убран дубль `deskEnsureCameraAfterPanelClose` в `closeDeskWindow`

## 3.41.4

- Камера: убран per-frame дожим курсора; в `/sp` ImGui не перехватывает мышь (DisableInput=true), только при HUD/panel
- Выход из `/sp`: `deskLeaveSpectateMode()` — сброс cursor mode 0 на всех путях (hook, `/sp`, закрытие F7)

## 3.41.3

- Камера: `deskRestoreNormalGameCamera` / `deskRestoreGameCameraAfterPanel` — вне `/sp` режим 0, без принудительного `CMODE_LOCKCAM` после F7
- Сценарии: откат `reply` к текстам до 3.41.2; убраны выдуманные команды (`/respawn`, `/anim stop`, `05:00`)
- Отключены неподтверждённые сценарии (слёт отеля); keywords из chatlog и typo-layer сохранены

## 3.41.2

- Сценарии: typo-layer матчинга (`normalizeMatchTextTypo`), guards от ложных срабатываний (механик/бензин/кредиты/БП)
- `admin_report_desk_user.lua`: пересмотр keywords/replies по chatlog ARP Blue; +5 сценариев (спавн авто, слёт отеля, одежда, награды БП, застрял)
- Dev: `tools/analyze_chatlog_scenarios.py` — покрытие вопросов «как/где» по chatlog

## 3.41.1

- Аудит: единый `deskSyncSpectateState` / `deskSetPlayerSpectating`, chain хуков onSendChat/onSendCommand/onPlayerQuit, watchdog 12 с
- Конфиг: сохранение `*_ctrl/shift/alt` для читов; маркер/tp бинды в runtime + guard СКМ vs hotkey окна
- Lifecycle: `deskUninstall` + `report_desk_app.unload()`; prune `profLineSeen`; UTF-8-safe split `/ans`
- UI: `deskShouldBlockGameInput` при открытой панели; `hideDeskWindowForCapture` при назначении биндов

## 3.41.0

- Модуляризация: монолит разбит на `report_desk_*.lua`, entry `admin_report_desk.lua` (~15 строк)
- Камера /sp: убран опрос 1.5 с, единый `deskSpectatingNow()`, guard переустановки хуков 12 с
- Changelog вынесен из шапки скрипта; чеклист регрессии: `docs/REGRESSION_CHECKLIST.md`

## Historical (from monolith header)

- v3.18.0: ревизия чата/ingest — без [A], единый ans wire, верстка пузырей, очистка поля
- v3.18.1: AirBreak — исправлен вектор strafe (A/D не инвертированы)
- v3.20.0: фикс ans в чужой тред; компактный HUD спектейта слева; /sp id + /st
- v3.21.0: F8 в игру (скриншот); /st без окна — HUD; админ-ответы не теряются в poll
- v3.21.1: блок ввода игры при F7; парсинг предупреждений /st
- v3.21.2: ←/→ переключение /sp по ID; жёстче блок чата на T при F7
- v3.21.3: Shift+Alt в чате не жмёт кнопки UI (NoNav, фокус)
- v3.21.4: Shift/Alt в SAMP-чате (/sp) не жмёт textdraw-меню админа
- v3.21.5: ←/→ /sp снова работают (щит только при реальном вводе в чат)
- v3.21.6: HUD /sp при открытом чате; ←/→ приоритет над меню админа
- v3.21.7: фикс WM (падение скрипта); ←/→ /sp в модуле spectate
- v3.21.8: курсор при SAMP-чате (/sp); цвет ника по фракции из /st
- v3.21.9: белый ник безработного; HUD /st после выхода из /sp (настройка)
- v3.22.0: админ-действия в треде; камера /sp не блокируется HUD
- v3.23.0: burst-прогрев каталога, DDS/bytes prefetch, round-robin warmup
- v3.23.1: HUD-индикатор прогрева каталога (спиннер, ETA, не пугает лагами)
- v3.23.2: солидный HUD прогрева в стиле панелей Report Desk
- v3.24.1: фикс индикатора прогрева — DrawList + не грузить в main loop без отрисовки
- v3.24.2: индикатор прогрева внизу по центру — «готовлю картиночки» + полоса
- v3.24.3: индикатор прогрева — только текст и полоса, без счётчиков
- v3.24.5: фикс лимита 200 local — catWarmup table; escape config в настройках
- v3.24.6: удалён чекер; фикс читов (WH без Alt, GM/WH после спавна)
- v3.24.7: сценарии — кнопки под сообщением (ImGui), skip_if_report_id в UI
- v3.24.8: фикс «14» в текстах (\x2014→\x97); Следить справа от пузыря
- v3.24.9: сценарии/правила/мат — отдельный UTF-8 admin_report_desk_user.lua
- v3.25.0: фикс user-конфига — не затирать пустым; UTF-8 только из user.lua
- v3.26.0: UX — чистый UI без техподписей; вкладка Читы карточками; AB Q/E сессия
- v3.27.0: вкладка Настройки — карточки, понятные подписи, аккуратный бинд окна
- v3.28.0: мат — словарь в report_desk_profanity_words.lua; настройки в Настройках; точное слово
- v3.29.0: кнопка «Техничка» под полем ввода (рядом с GG), текст настраивается
- v3.30.0: HUD мата — сверху по центру, фракция/цвет ника, метка Чат или /me
- v3.30.1: аудит — HUD при открытом окне, O(1) словарь, [G]-чат, фикс словаря
- v3.30.2: аудит модулей — spectate cache/hooks, tex prefetch, veh/skins scroll
- v3.30.3: фикс загрузки — лимит 200 local variables в Lua
- v3.31.1: фикс загрузки — until зарезервированное слово в Lua
- v3.31.2: мат — /me /do через onSendCommand; парсер [G] и OOC; меньше ложных /me
- v3.31.3: фикс карточек Настройки/Читы — высота 0 ломала ImGui layout
- v3.32.0: Настройки/Читы — плоские секции, ровные строки; AB Q/E с удержанием
- v3.32.1: мат — ARP /me «- текст (Nick)[id]», рации [J]/[PC], без ложных /me
- v3.32.2: фикс F7 при клике в диалог; бинды — кнопка + превью; маркер — галочка СКМ
- v3.33.0: мат — ARP /me/-строка, без хука команд; компактный HUD справа сверху
- v3.33.1: фикс загрузки — лимит 200 local (deskCache/deskInputState)
- v3.33.2: ingest — не ловить (S2 lvl); F7/крестик закрытия; читы — компактнее
- v3.33.3: hotkey toggle как раньше; /sp камера; форма Настройки/Читы
- v3.33.4: HUD мата — сверху по центру, чуть заметнее
- v3.33.5: форма — чекбоксы; бинд «из списка»; окно закрывается при назначении
- v3.33.6: подсказка тегов {datetime} в автоответах и быстрых ответах
- v3.33.7: F7/бинд окна — toggle в WM; компактные строки биндов
- v3.34.0: F7 закрывает окно; мат без повтора; /sp камера; маркер в авто; вкладка Быстрые ответы; бинды без закрытия окна
- v3.34.1: фикс бинда M4/M5 — окно не мигает (двойной WM_KEYDOWN + XBUTTON)
- v3.34.2: мат — не всплывает на каждый репорт/чат без совпадения со словарём
- v3.34.3: HUD мата — перенос длинного текста, фон на всю карточку
- v3.34.4: фикс hotkey — снова закрывает окно (M5/F7), без двойного toggle
- v3.34.5: фикс загрузки — лимит 200 local (состояние в deskCache/deskInputState)
- v3.34.6: чат — вниз при выборе треда и при новых сообщениях выбранного треда
- v3.34.7: UI — чипы слов, toggle-переключатели, ровные панели Настройки/Читы
- v3.34.8: фикс краша Настройки (PopStyleVar); фон карточек Читы/Настройки под текстом
- v3.34.9: сценарии — целые слова (не «тир» в ремонтировать); редактор проще; панели без DrawList
- v3.39.2: чат — исходящие только в тред получателя; /sp камера без повторного restore
- v3.39.3: ingest — «только репорты» снова берёт [PC]/[S] из чата; live при первом poll; быстрее при F7
- v3.39.4: ingest — канал репорта [M] (как PC/S)
- v3.40.0: продакшн — ingest PC/S/M, безопасные треды/ans при смене ID, сценарии UI
- v3.40.1: tooltip сценариев — русский текст (без смешения CP1251/UTF-8)
- v3.40.2: F7 закрыт — камера сразу; фикс крестика ImGui (close не срабатывал)
- v3.39.1: скины — радиус: кнопки ± и значение в метрах (по умолчанию 20)
- v3.39.0: сценарии — кнопки на всех подходящих сообщениях; подсказка с текстом ответа
- v3.38.2: /sp — камера без вмешательства при закрытой панели; LOCKCAM только после UI
- v3.38.1: /sp — дожим LOCKCAM/DISABLED при сбое режима; запоминание камеры после входа в /sp
- v3.38.0: /sp — единая политика ввода: UI при открытой панели, камера при закрытой
- v3.37.0: /sp — режим камеры SAMP запоминается; поддержка без костылей wait()
- v3.36.1: /sp — после закрытия панели снова LOCKCAM (один раз, не в цикле)
- v3.36.0: /sp — окно скрыто = скрипт не трогает камеру и кнопки (без LOCKCAM-циклов)
- v3.35.10: /sp+F7 — LOCKCAM при открытой панели; lockPlayer только в спектейте
- v3.35.9: ввод — F7 всегда закрывает окно; /sp камера без ломания UI
- v3.35.8: /sp — CMODE_LOCKCAM для камеры; ImGui DisableInput в спектейте
- v3.35.7: автоответы — не слать накопившиеся репорты после AFK (poll без auto, дедуп строк)
- v3.35.6: спектейт — мышь/курсор не блокируются при /sp и «Следить» (F7 открыт или закрыт)
- v3.35.5: slap/tr только в шапке диалога, не у каждого сообщения
